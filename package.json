{
  "name": "decors",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "react-scripts start",
    "build-css": "tailwindcss build -i ./src/input.css -o ./src/styles.css",
    "watch-css": "tailwindcss build -i ./src/input.css -o ./src/styles.css --watch",
    "dev": "concurrently \"npm run watch-css\" \"npm start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.3.2"
  },
  "dependencies": {
    "tailwind": "^4.0.0"
  }
}
